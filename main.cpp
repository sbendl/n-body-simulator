#include <iostream>
#include <random>

using namespace std;

int main() {
    int NUM_BODIES = 1000;
    double MIN_SPEED = 100;
    double MAX_SPEED = 50000;
    double GRAV_CONST = 6.67408 * pow(10, -11);
    int BASE_PART_MASS = 1000000;

    // Set up initial position and velocity

    for (int i = 0 ; i < )

}